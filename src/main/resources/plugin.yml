name: ${project.artifactId}
main: ${project.groupId}.questengine.QuestEngine
version: ${project.version}
website: ${project.url}
description: The core of the MCME quest engine
author: Dallen
depend: [ProtocolLib]
commands:
  Quests:
    description: Core quests command
    usage: |
            /Quests join
            /Quests leave
            /Quests purge
            /Quests reload
            /Quests save
